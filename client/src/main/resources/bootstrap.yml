spring:
  application:
    name: client
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: CONFIG #1
eureka:
  client:
    service-url:
      defaultZone: http://172.17.1.170:8080/eureka/

