spring:
  application:
    name: zuul

eureka:
  instance:
    non-secure-port: ${server.port:19883}
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8111}/eureka/
